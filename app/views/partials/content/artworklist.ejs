<article class="sidebar">
  <h1 class="sidebar-title">Trending Causes</h1>
  <p class="sidebar-body">Browse through the currently most popular and trending user stories. Help them out or get inspired!</p>
  <div class="pixgrid clearfix">
    <% if (artwork.length > 0) { %>
      <% for (i=0; i< artwork.length; i++) { %>
        <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>">
        <% } %>
        <% } %>
      </div>
    </article>
